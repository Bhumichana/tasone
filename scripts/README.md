# Scripts สำหรับจัดการวันหมดอายุของ Batch

Scripts เหล่านี้ใช้สำหรับอัปเดตวันหมดอายุของ Batch ที่บันทึกไว้ก่อนหน้านี้โดยไม่ได้ระบุวันหมดอายุ

## 📋 รายการ Scripts

### 1. `list-batches-no-expiry.js`
แสดงรายการ Batch ทั้งหมดที่ยังไม่มีวันหมดอายุ

**วิธีใช้:**
```bash
node scripts/list-batches-no-expiry.js
```

**ผลลัพธ์:**
- แสดงตารางรายการ Batch ที่ไม่มีวันหมดอายุ
- แสดง Batch Number, ชื่อวัตถุดิบ, วันที่รับเข้า, สต็อกปัจจุบัน

---

### 2. `update-batch-expiry.js`
อัปเดตวันหมดอายุของ Batch ทีละรายการ

**วิธีใช้:**
```bash
node scripts/update-batch-expiry.js <batch-number> <expiry-date>
```

**ตัวอย่าง:**
```bash
# รูปแบบ YYYY-MM-DD
node scripts/update-batch-expiry.js DEMO-2025-10-01 "2026-12-31"

# รูปแบบ DD/MM/YYYY
node scripts/update-batch-expiry.js DEMO-2025-10-02 "31/12/2026"

# รูปแบบ DD-MM-YYYY
node scripts/update-batch-expiry.js DEMO-2025-10-03 "31-12-2026"
```

**ผลลัพธ์:**
- อัปเดตวันหมดอายุใน `RawMaterialBatch`
- อัปเดตวันหมดอายุใน `DealerStock` ทั้งหมดที่เกี่ยวข้อง
- แสดงสรุปผลการอัปเดต

---

### 3. `update-multiple-batches.js` ⭐ แนะนำ
อัปเดตวันหมดอายุของหลาย Batch พร้อมกัน (เหมาะกับการอัปเดตข้อมูลเก่าจำนวนมาก)

**วิธีใช้:**

1. **แก้ไขไฟล์** `scripts/update-multiple-batches.js`

2. **เพิ่มรายการ Batch** ที่ต้องการอัปเดตใน `BATCH_UPDATES`:

```javascript
const BATCH_UPDATES = [
  { batchNumber: 'DEMO-2025-10-01', expiryDate: '2026-12-31' },
  { batchNumber: 'DEMO-2025-10-02', expiryDate: '2026-12-31' },
  { batchNumber: 'DEMO-2025-10-03', expiryDate: '2027-01-15' },
  // เพิ่มรายการอื่นๆ ตามต้องการ
]
```

3. **รัน script:**
```bash
node scripts/update-multiple-batches.js
```

**ผลลัพธ์:**
- อัปเดต Batch ทั้งหมดที่ระบุในรายการ
- แสดงสรุปผลการอัปเดต (สำเร็จ/ล้มเหลว)
- แสดงจำนวน DealerStock ที่ได้รับการอัปเดต

---

## 🔧 ขั้นตอนการใช้งาน

### วิธีที่ 1: อัปเดตทีละ Batch (เหมาะกับ Batch น้อยๆ)

```bash
# 1. ดูรายการ Batch ที่ไม่มีวันหมดอายุ
node scripts/list-batches-no-expiry.js

# 2. อัปเดตทีละ Batch
node scripts/update-batch-expiry.js DEMO-2025-10-01 "2026-12-31"
node scripts/update-batch-expiry.js DEMO-2025-10-02 "2026-12-31"
```

### วิธีที่ 2: อัปเดตหลาย Batch พร้อมกัน (แนะนำ) ⭐

```bash
# 1. ดูรายการ Batch ที่ไม่มีวันหมดอายุ
node scripts/list-batches-no-expiry.js

# 2. แก้ไขไฟล์ update-multiple-batches.js ให้ตรงกับรายการที่ต้องการ
# (เพิ่ม batch number และวันหมดอายุใน BATCH_UPDATES)

# 3. รัน script
node scripts/update-multiple-batches.js

# 4. ตรวจสอบผลลัพธ์
node scripts/list-batches-no-expiry.js
```

---

## 📅 รูปแบบวันที่ที่รองรับ

Scripts รองรับรูปแบบวันที่หลายแบบ:

- `YYYY-MM-DD` → `2026-12-31`
- `DD/MM/YYYY` → `31/12/2026`
- `DD-MM-YYYY` → `31-12-2026`

---

## ⚠️ ข้อควรระวัง

1. **สำรองข้อมูล** ก่อนรัน script (แนะนำให้สำรอง `prisma/dev.db`)

2. **ตรวจสอบวันหมดอายุ** ให้แน่ใจว่าวันหมดอายุถูกต้อง

3. **ไม่สามารถย้อนกลับได้** - เมื่ออัปเดตแล้ว ต้องอัปเดตใหม่ถ้าต้องการเปลี่ยน

4. **ผลกระทบ:**
   - อัปเดตข้อมูลใน `RawMaterialBatch` (ที่ Head Office)
   - อัปเดตข้อมูลใน `DealerStock` (ที่ Dealer ทุกราย)

---

## 💡 Tips

- ใช้ `list-batches-no-expiry.js` เพื่อดูว่ามี Batch ไหนที่ยังไม่มีวันหมดอายุบ้าง
- ถ้ามี Batch จำนวนมาก แนะนำใช้ `update-multiple-batches.js`
- สามารถรัน script ซ้ำได้ (จะอัปเดตทับค่าเดิม)
- หลังจากอัปเดตเสร็จ ให้รีเฟรชหน้า Stock ของ Dealer เพื่อดูผลลัพธ์

---

## 🐛 การแก้ปัญหา

**ปัญหา:** `Cannot find module '@prisma/client'`
```bash
npm install
```

**ปัญหา:** วันที่ไม่ถูกต้อง
- ตรวจสอบรูปแบบวันที่ (YYYY-MM-DD, DD/MM/YYYY, หรือ DD-MM-YYYY)
- ใส่เครื่องหมาย " " ครอบวันที่

**ปัญหา:** ไม่พบ Batch
- ตรวจสอบ Batch Number ว่าถูกต้องหรือไม่
- รัน `list-batches-no-expiry.js` เพื่อดูรายการที่มี

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย สามารถติดต่อได้ที่ทีมพัฒนา
